import 'package:flutter/material.dart';

const primaryColor = Color(0xFFE0857E);
const baseURL = "https://brownonions-002-site2.ftempurl.com/api";
const apiKey = "mobileapi19042024";
const chefID = 3;
const userID = -1;
const statusID = -1;

var textFieldBorder = OutlineInputBorder(
  borderRadius: BorderRadius.circular(20),
  borderSide: const BorderSide(color: primaryColor),
);
